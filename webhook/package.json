{
  "name": "stripe-formsubmit-webhook",
  "version": "1.0.0",
  "description": "Webhook to relay Stripe payment events to FormSubmit via email",
  "main": "stripe-webhook.js",
  "scripts": {
    "start": "node stripe-webhook.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2"
  }
}
